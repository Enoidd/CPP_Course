/*

VECTOR OPERAZIONI

*/

#include <iostream>
#include <vector>
using namespace std;

int main(){

    vector<int> v(5); // inizialiazzati a 0

    // Input
    v.push_back(1);
    v.push_back(2);
    v.push_back(3);
    v.push_back(4);
    v.push_back(5);

    // push_back aggiunge in fondo.
}